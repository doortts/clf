# clfl 에이전트 규칙

## 문서 작성: 타이핑 가능한 문자만 쓴다

문서에는 **키보드로 바로 입력할 수 있는 문자만** 쓴다. 특수문자 입력기나 복사/붙여넣기
없이 그대로 타이핑할 수 있어야 한다.

이 규칙은 저장소 안의 모든 마크다운, 코드 주석, 커밋 메시지에 적용된다.

### 허용

- 한글
- ASCII 인쇄 가능 문자 (영문, 숫자, `!"#$%&'()*+,-./:;<=>?@[\]^_` `` ` `` `{|}~`)
- 마크다운 문법 문자

### 쓰지 않는 문자와 대체

문자를 직접 적으면 규칙 위반이 되므로 코드포인트로 지정한다.

| 코드포인트 | 이름 | 대신 쓴다 |
|---|---|---|
| U+2014 | em dash | `-` (앞뒤 공백), 또는 문장을 나눈다 |
| U+2013 | en dash | `-` |
| U+2192 | 오른쪽 화살표 | `->` |
| U+2190 | 왼쪽 화살표 | `<-` |
| U+00B7 | 가운뎃점 | `, ` 또는 `/` |
| U+2026 | 말줄임표 | `...` |
| U+00A7 | 섹션 기호 | `3절` 처럼 번호만 |
| U+26A0 | 경고 기호 | `주의:` |
| U+2705 | 체크 기호 | `O` 또는 `yes` |
| U+274C | 엑스 기호 | `X` 또는 `no` |
| U+2264 U+2265 | 부등호 기호 | `<=`, `>=` |
| U+2460 이상 | 원문자 숫자 | `1.` 또는 `(1)` |
| U+2500 이상 | 박스 드로잉 | ASCII 트리 (`|`, `+--`, `` ` ``) |
| U+2018 U+2019 U+201C U+201D | 굽은 따옴표 | `'`, `"` |
| U+FE0F | 변이 선택자 | 지운다 (이모지 잔재) |
| U+00A0 | 줄바꿈 없는 공백 | 일반 공백 |

이모지는 쓰지 않는다.

### 예외

- 원문 인용: 외부 문서나 코드의 문자열을 그대로 인용할 때는 원문을 보존한다.
  인용임을 알 수 있게 코드 블록이나 백틱 안에 둔다.
- 이 표의 코드포인트 표기 자체.

### 검사

커밋 전에 실행한다. 출력이 없으면 통과다.

```bash
perl -CSD -ne 'while(/([^\x00-\x7F])/g){ $c=$1; next if $c=~/\p{Hangul}/; printf "%s:%d: U+%04X %s\n",$ARGV,$.,ord($c),$c }' $(git ls-files '*.md')
```

---

## 커밋

- Conventional Commits 형식. 스코프는 디렉토리나 모듈 이름.
- 본문은 무엇을 왜 바꿨는지 적는다.
- **작성 도구를 밝히는 trailer를 넣지 않는다.** `Co-Authored-By`, 생성 도구 표기 모두
  해당한다. author/committer는 로컬 git config 값을 그대로 쓴다.
